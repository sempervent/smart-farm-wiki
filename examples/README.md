# Examples

Isolated copies demonstrating ingest and wiki structure. **Not** wired into the live `wiki/` or `raw/` trees—copy patterns into your vault when learning.

- `examples/raw/` — sample inbox/processed layout
- `examples/wiki/` — miniature pages showing frontmatter and linking

See the main [`wiki/`](../wiki) and [`raw/`](../raw) directories for the canonical template content.
